#include<iostream>
using namespace std;
main() {
    char hoten[2],hoten2[2];
    gets(hoten);
    gets(hoten2);
        if(hoten==hoten2) {
            cout<<"two people have the same name";
        } else {
            cout<<"two people don't have the same name";
        }

}